{
  "name": "armor",
  "version": "1.0.0",
  "bin": {
    "armor": "./bin/armor"
  },
  "type": "module",
  "scripts": {
    "link-config": "./scripts/link-config.sh",
    "check": "tsc --noEmit",
    "lint": "oxlint --tsconfig tsconfig.json extensions/",
    "lint:fix": "oxlint --tsconfig tsconfig.json --fix extensions/",
    "format": "oxfmt --write .",
    "format:check": "oxfmt --check ."
  },
  "devDependencies": {
    "bun-types": "latest",
    "oxfmt": "^0.43.0",
    "oxlint": "^1.58.0",
    "typescript": "^6.0.2"
  }
}
